    <div class="custom-wrapper pure-g" id="menu">
        <div class="pure-u-1 pure-u-md-1-3">
            {# Logo here #}
            <div class="pure-menu">
                <a class="logo-link" href="{{ home_url|e }}">
                    <img src="{{ url('theme://images/logo.svg')|e }}" alt="Logo" width="30" height="30" />
                    <div class="logo-text">
                    <span id="text-mat">mat</span><span id="text-lib">lib</span>
                    </div>
                </a>
            </div>
            {# Navigation here #}
            <div class="pure-u-1 pure-u-md-1-3">
                <div class="pure-menu pure-menu-horizontal custom-can-transform">
                {% block header_navigation %}
                    <ul class="pure-menu-list">
                    {% include 'partials/navigation.html.twig' %}
                    </ul>
                {% endblock %}
            </div>
        </div>
    </div>